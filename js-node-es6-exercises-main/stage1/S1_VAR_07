function toNumberOrNull(x) {
  const n = +x;
  if (typeof x === "string" && !Number.isNaN(n)) {
    return n;
  }
  return null;
}

const tests = ["12", "12.5", " 12 ", "12x", ""];

for (const t of tests) {
  console.log(JSON.stringify(t), "=>", toNumberOrNull(t));
}
